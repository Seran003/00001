# Project Requirements Organization

This folder is split for focused delivery:

- `mvp/` -> implementation-first documents and data model for current build scope.
- `reference_artifacts/` -> source materials, transcripts, and broader context docs.

If you are implementing now, start in `mvp/`.
